import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    margin: 10,
  },

  text: {
    margin: 3,
    fontWeight: "600",
  },
});

export default styles;
